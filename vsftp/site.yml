---

- name: install ftpserver
  hosts: ftpserver
  remote_user: ansible
  become: yes
  become_method: sudo
  #become_user: ansible

  tasks:
  - name: restart vsftp
    systemd:
      name: vsftpd.service
      state: restarted
  #roles:
  #  - vsftp
