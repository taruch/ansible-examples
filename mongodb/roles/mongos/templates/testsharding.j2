const people = ["Marc", "Bill", "George", "Eliot", "Matt", "Trey", "Tracy", "Greg", "Steve", "Kristina", "Katie", "Jeff"];

for (let i = 0; i < 100000; i++) {
  db.{{ mongo_test_collection }}.insertOne({
    name: people[Math.floor(Math.random() * people.length)],
    user_id: i,
    boolean: [true, false][Math.floor(Math.random() * 2)],
    added_at: new Date(),
    number: Math.floor(Math.random() * 10001),
  });
}

db.{{ mongo_test_collection }}.createIndex({ number: 1 });

