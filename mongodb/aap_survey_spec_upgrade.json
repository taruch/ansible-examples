{
  "name": "MongoDB Upgrade Survey",
  "description": "Target version for the rolling MongoDB major-version upgrade",
  "spec": [
    {
      "question_name": "Target MongoDB Version",
      "question_description": "MongoDB major version to upgrade to. Must be exactly one major version ahead of mongodb_version in group_vars/all/vars.yml (e.g. 7.0 -> 8.0).",
      "required": true,
      "type": "text",
      "variable": "mongodb_upgrade_version",
      "min": 0,
      "max": 32,
      "default": "8.0",
      "choices": "",
      "new_question": true
    }
  ]
}
