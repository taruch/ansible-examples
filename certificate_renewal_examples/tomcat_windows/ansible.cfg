[defaults]
# Located at the tomcat_windows/ level so ansible-navigator (which sets cwd to
# the project root inside the EE) finds it. Both demo_host_init.yml and
# renew_tomcat_cert.yml share this roles_path.
roles_path = ./roles
