---
# Source vault_keystore_password from ansible-vault or an AAP Vault credential.
# Example: ansible-vault encrypt_string 'changeit' --name vault_keystore_password
vault_keystore_password: "REPLACE_ME"
