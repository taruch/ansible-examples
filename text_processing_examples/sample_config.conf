# Sample configuration file used to demonstrate ansible.builtin.lineinfile
# managing a block of text via insertafter/insertbefore anchors.

[general]
app_name = demo-app
environment = production

# BEGIN ANSIBLE MANAGED BLOCK
# END ANSIBLE MANAGED BLOCK

[logging]
log_level = info
