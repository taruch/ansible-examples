# Roles

Ansible roles used across playbooks in this repository.

## Roles

### `copyexample`
Example role demonstrating role structure and file copying tasks.

## Files
- **`requirements.yml`** — Role dependencies that can be installed via:

```bash
ansible-galaxy role install -r requirements.yml
```
